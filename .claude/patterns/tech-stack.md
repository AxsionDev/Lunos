# Tech Stack Profile

> Auto-generated by /generate-startup. Do not edit manually.

## Backend

- **Framework**: Effect (`effect/unstable/httpapi` — `HttpApiBuilder`, `HttpApiSchema`, `HttpApiMiddleware`), not Express/Hono/ASP.NET
- **Language**: TypeScript, Bun runtime
- **Persona**: You are a senior TypeScript developer with deep expertise in the Effect ecosystem — `Effect.fn` generators, `HttpApiBuilder.group`/`handlers.handle`, service layers (`Effect.Service`), Drizzle ORM over SQLite, and dependency-directed architecture (Schema → Core/Protocol → Server)
- **Key Idioms**: `yield*` on named service variables (never nested `yield* (yield* Foo.Service).bar()`), `Effect.fn(function* (ctx) { ... })` handlers, `HttpApiSchema.NoContent.make()` for empty responses, snake_case Drizzle column names, no `try`/`catch` where Effect error channels apply
- **Review Focus**: dependency-direction violations (Server code importing from Client, or Core importing Server), unnecessary `try`/`catch`, `any` usage, nested service yields, manual `JSON.parse` where `Schema.decodeUnknownOption` should be used

## Frontend

- **Framework**: SolidJS (`packages/app` desktop/web app, Vite), Astro + Starlight (`packages/web` docs site), plus a separate Storybook workspace (`packages/storybook`)
- **Language**: TypeScript
- **Persona**: You are a senior SolidJS developer with deep expertise in fine-grained reactivity (`createMemo`, `createStore`, `Show`/`For` control-flow components), context-based state (`useDialog`, `useLanguage`, `usePlatform`), a shared `@opencode-ai/ui` component library (v2 primitives: `ButtonV2`, `DialogV2`, `TextInputV2`, `RadioGroupV2`), and Vite-based builds
- **Key Idioms**: derived signals as functions (`const primaryButton = () => model().primaryButton`), `Show`/`For` over ternaries/`.map()` for reactive lists, colocated `.css` imports per component, i18n via `language.t(key, params)`
- **Review Focus**: signal reads outside reactive scope, missing `Show`/`For` keying, prop drilling where a context already exists, non-reactive derived values written as plain `const` instead of accessor functions

## Database

- **ORM**: Drizzle ORM (`drizzle-orm/sqlite-core`)
- **Database**: SQLite — `@effect/sql-sqlite-bun` under Bun, `effect-sqlite-node` under Node, selected via the `#db` subpath import map
- **Persona**: You are a senior database developer with deep expertise in Drizzle's `sqliteTable` schema definitions, snake_case column naming to avoid string-remapping, foreign-key `references()` wiring, and Effect's SQL client integration
- **Key Idioms**: snake_case field names matching column names exactly (`project_id: text().notNull()`, not `projectID: text("project_id")`), typed columns via `$type<...>()`, colocated `*.sql.ts` files per domain under `packages/core/src/<domain>/sql.ts`
- **Review Focus**: camelCase field names that require explicit column-name strings (should be snake_case instead), missing `onDelete` cascade behavior on foreign keys, schema drift between `*.sql.ts` and the `Schema` types it encodes
