# lunos-community seed skills

A skill source (XCOD-105): `index.json` lists each skill and its files, in the format `skills.urls`
reads. The `lunos-community` manifest points at this directory through a commit-pinned
`raw.githubusercontent.com` URL, so what installs is exactly what was committed.
